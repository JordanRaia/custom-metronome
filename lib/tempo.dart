import 'package:flutter/material.dart';

class MyTempo extends StatelessWidget {
  const MyTempo({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('MyTempo');
  }
}